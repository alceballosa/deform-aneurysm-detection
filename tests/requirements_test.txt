# Testing dependencies for deformable aneurysm detection project

# Core testing framework
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-xdist>=3.0.0  # For parallel test execution
pytest-timeout>=2.1.0  # For test timeouts

# Configuration
yacs>=0.1.8  # For config testing

# Data handling (should already be in main requirements)
numpy>=1.21.0
pandas>=1.3.0
SimpleITK>=2.1.0

# Deep learning (should already be in main requirements)
torch>=1.10.0
torchvision>=0.11.0

# Detectron2 (should already be installed)
# Install separately: pip install 'git+https://github.com/facebookresearch/detectron2.git'

# Additional testing utilities
hypothesis>=6.0.0  # For property-based testing (optional)
